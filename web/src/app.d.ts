declare global {
  // Injected by Vite `define` from the workspace Cargo.toml version.
  const __HOARD_VERSION__: string;

  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export {};
