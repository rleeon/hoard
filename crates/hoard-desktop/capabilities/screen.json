{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "screen",
  "description": "Pro Hoard Screen: execute and drive the native overlay sidecar (hoard-screen). Dropped in by the Pro build; absent (and unused) in the community stub.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "hoard-screen", "sidecar": true, "args": true }]
    }
  ]
}
