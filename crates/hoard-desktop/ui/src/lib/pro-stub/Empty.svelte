<script lang="ts">
  // Inert placeholder used by the community build. The real Pro components live
  // in the private hoard-pro repo and replace this via the `$pro` alias in
  // official/Pro builds. Renders nothing — gated routes fall back to ProGate.
</script>
